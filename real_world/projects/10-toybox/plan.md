# 10 -- Toybox probe plan

## Source snapshot

- Project: Toybox
- Upstream URL: <https://github.com/landley/toybox>
- Clone/archive command: `git -c core.autocrlf=false clone --depth 1 https://github.com/landley/toybox upstream`
- Resolved commit or checksum: `36fc372482bc4eb8b96e546b5337d03bef341dcf`
- Date fetched: 2026-05-06

## Upstream build entry points

- Build files inspected: `README`, `Makefile`, `configure`, `scripts/single.sh`,
  `scripts/make.sh`, `main.c`, `toys.h`
- Smallest compile target: standalone applets via `scripts/single.sh`
- Smallest runnable target: `true false echo cat wc`
- Required generated config: `make defconfig` plus Toybox `generated/*`

## Baseline oracle

- Host compiler: `cc`
- Host compile command: `PREFIX=<build>/host/ CC=cc HOSTCC=cc scripts/single.sh true false echo cat wc`
- Host run command: wrapper-driven `true`, `false`, `echo`, `cat`, and `wc`
- Expected exit status: compared per applet against `rcc`
- Expected stdout/stderr: compared byte-for-byte against `rcc`

## rcc probe

- `rcc` compile command: `PREFIX=<build>/rcc/ CC=scripts/rcc-cc.sh scripts/single.sh true false echo cat wc`
- Link command, if separate: Toybox invokes `CC` for final link inside
  `scripts/make.sh`
- Run command: wrapper-driven `true`, `false`, `echo`, `cat`, and `wc`
- Expected comparison: exit status, stdout, and stderr match host baseline
- Current result: blocked before object emission; see TBX-001 below

## Allowed local adaptation

- Wrapper scripts: `scripts/run-applet-smoke.sh`, `scripts/rcc-cc.sh`
  (`rcc-cc.sh` delegates Toybox's stdin-only preprocessor/library probes to
  the host compiler; ordinary applet object compilation and final linking still
  go through `rcc`)
- Build-script-only patches: none
- Generated config files: generated by Toybox under ignored `upstream/`

## Disallowed adaptation checklist

- [x] No upstream `.c` file modified
- [x] No upstream `.h` file modified
- [x] No failing upstream test deleted
- [x] No runtime oracle weakened to hide an `rcc` bug

## Failure log

Record each failure here before fixing anything.

| ID | Command | Symptom | Classification | Follow-up task |
| --- | --- | --- | --- | --- |
| TBX-001 | `LLVM_SYS_181_PREFIX=/usr/lib/llvm-18 bash real_world/projects/10-toybox/scripts/run-applet-smoke.sh` | Host applets build, but `rcc` stops while compiling the first Toybox applet source set. After the C11 transition, `_Noreturn` and `sigjmp_buf` no longer appear in the failure log; current diagnostics include `timer_t`, `SIGKILL`, `SIGWINCH`, `stpcpy`, `syscall`, `netinet/tcp.h`, and `timespec/timeval` redeclaration gaps. | hosted Linux header/POSIX surface | `tasks/16-linux-glibc-compat/25-toybox-applet-hosted-surface.md` |

## Exit criteria

- [x] Host baseline built
- [x] Host baseline run recorded, when applicable
- [x] `rcc` build attempted
- [ ] `rcc` run compared with baseline, when applicable
- [ ] Compiler bugs have minimized regressions
- [x] `RESULTS.md` updated
