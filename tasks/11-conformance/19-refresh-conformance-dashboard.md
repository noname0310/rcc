> ✓ done — 2026-05-05

# 11-19: Refresh conformance dashboard and stale fail docs

**Phase:** 11-conformance    **Depends on:** 11-18    **Milestone:** M7

## Goal
Remove stale `Fail` rows from `docs/conformance.md` / `docs/conformance.json`
when the current compiler no longer reproduces them, and make any remaining
failure numbers clearly scoped to non-required exploratory suite modes.

## Scope
- In: regenerate `docs/conformance.json` from the current required suite set,
  rerender the autogenerated dashboard, and update hand-written stale baseline
  text in `docs/conformance.md`.
- In: explicitly distinguish chibicc `stage-1-3` (required gate) from chibicc
  default full-suite compile mode (exploratory, extension-heavy).
- Out: fixing all chibicc full-suite default failures; those are feature work
  for later GNU/C11/platform-extension tasks.

## Deliverables
- Locally regenerated `docs/conformance.json` (ignored generated report).
- Updated `docs/conformance.md` with no stale required-suite fail claims.
- Verification commands recorded in the task completion note.

## Acceptance
- Required dashboard suites show 0 non-xfailed failures.
- `c-testsuite` report is current and has 0 fail.
- `chibicc` `stage-1-3` report is current and has 0 fail.
- Any mention of chibicc full-suite failures is labelled exploratory / not the
  current required gate.
- No new xfail entries are added.

## Completion note
- Regenerated `docs/conformance.json` from WSL with
  `c-testsuite`, chibicc `stage-1-3`, `tcc-tests2`, and
  `llvm-test-suite`: 317 cases, 300 pass, 13 xfail, 4 skip, 0 fail.
- Rerendered `docs/conformance.md` from that JSON.
- Replaced stale `control.c` / `function.c` failure claims with the current
  3 pass / 0 fail stage status.
- Kept the full chibicc default compile failure count only as an explicitly
  exploratory extension-heavy baseline.

## References
- `docs/conformance.md`
- `crates/rcc_conformance/src/bin/rcc_conformance_run.rs`
- `tasks/11-conformance/18-xfail-shrink.md`
